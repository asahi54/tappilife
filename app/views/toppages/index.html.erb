<div class="center jumbotron">
  <div class="text-center">
    <h1>TappiLife</h1>
    <%if user_signed_in? %>

    <%end%>
  </div>
</div>
<div class="text-center">
    意外とカロリーの高いタピオカドリンク・・・<br>
    あなたの飲むドリンクがどのくらいのカロリーなのか<br>
    チェックして記録をとれば<br>
    健康で幸せな生活へ一歩近づける...かも？
</div>

<div class="container mt-3">
  <div class="text-center mx-auto">
    <%= form_with(url: drinks_path, local: true, method: :get) do |f| %>
      <%= f.select :shop_id, options_for_select([["幸福堂", "1"], ["Gong cha", "2"], ["春水堂", "3"]]), class: 'form-control' %>
      <%= f.submit 'でタピる', class: 'btn btn-warning ml-1' %>
    <% end %>
  </div>  
</div>
<div class="container">
  <% if user_signed_in? %>
    <div class="text-center mt-2">
      <%= link_to 'Myタピ記録へ', mycalories_user_path(current_user), class: 'btn btn-lg btn-primary' %>
    </div>
  <% else %>  
    <div class="text-center">
      Myタピ記録はログイン後に利用可能です👇👇
    </div>
    <div class="text-center">
      <%= link_to '登録する', new_user_registration_path, class: 'btn btn-lg btn-primary' %>
      <%= link_to 'ログイン', new_user_session_path, class: 'btn btn-lg btn-primary' %>
    </div>
  <% end %>
</div>