<%= form_with(model: current_user.takes.find_by(drink_id: drink.id), method: :delete, local: true,) do |f| %>
    <%= hidden_field_tag :drink_id, drink.id %>
    <%= f.submit '削除', class: 'btn btn-danger' %>
<% end %>
